{
  "name": "skimroute-extension",
  "version": "1.3.9",
  "private": true,
  "description": "SkimRoute Chrome MV3 extension local build and regression tests.",
  "scripts": {
    "build": "node scripts/build-assets.js",
    "check:assets": "node scripts/build-assets.js --check",
    "test": "node scripts/run-node-tests.js",
    "test:browser": "playwright test tests/browser/skimroute.browser.test.js --config=tests/browser/playwright.config.js"
  },
  "devDependencies": {
    "@playwright/test": "1.60.0"
  }
}
